describe("Meu primeiro teste com Jasmine e NodeJS", function () {
  it("deve executar meu primeiro teste", function () {
    expect(true).toEqual(true);
  });

  it("deve executar meu primeiro escuta do teste", function () {
    expect(1).toEqual(1);
  });

  it("deve executar meu primeiro escuta do teste", function () {
    expect(2).toEqual(2);
  });
});
